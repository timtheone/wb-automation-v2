{
  "name": "@wb-automation-v2/backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file ${ENV_FILE:-../../.env.local} --hot src/index.ts",
    "start": "bun --env-file ${ENV_FILE:-../../.env} src/index.ts",
    "sync:content:run": "bun --env-file ${ENV_FILE:-../../.env} src/run-sync-content-job.ts",
    "wb:token:expiration:run": "bun --env-file ${ENV_FILE:-../../.env} src/run-wb-token-expiration-job.ts",
    "openapi:write": "bun --env-file ${ENV_FILE:-../../.env.local} src/write-openapi.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.3",
    "@hono/zod-openapi": "^1.2.2",
    "@wb-automation-v2/core": "workspace:*",
    "@wb-automation-v2/db": "workspace:*",
    "hono": "^4.12.0",
    "pg-boss": "^12.13.0",
    "pino": "^10.3.1",
    "zod": "^4.3.6"
  }
}
